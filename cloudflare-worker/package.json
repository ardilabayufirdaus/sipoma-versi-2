{
  "name": "sipoma-cloudflare-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy untuk SIPOMA API",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "wrangler dev --test"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
